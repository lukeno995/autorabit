@isTest
global class OF_TTM_WS_IN_Notify_CalloutMock implements WebServiceMock {
	
	public static boolean isACK;
	
   global void doInvoke(
           Object stub,
           Object request,
           Map<String, Object> response,
           String endpoint,
           String soapAction,
           String requestName,
           String responseNS,
           String responseName,
           String responseType) {

        // Create response element from the autogenerated class.
        if(isACK){
	        OF_TTM_WS_IN_Notify.TTM_NotifyResponse ttm_response = new OF_TTM_WS_IN_Notify.TTM_NotifyResponse();
	        ttm_response.ESITO = '0';
	        // Populate response element.
	        // Add response element to the response parameter, as follows:
	        response.put('response_x', ttm_response);
        }else{
		    OF_TTM_WS_IN_Notify.TTM_NotifyResponse ttm_response = new OF_TTM_WS_IN_Notify.TTM_NotifyResponse();
		    ttm_response.ESITO = '1';
		    // Populate response element.
		    // Add response element to the response parameter, as follows:
		    response.put('response_x', ttm_response);
        } 
   }
}